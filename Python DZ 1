# Белочки и орешки - 1
# N белочек нашли K орешков и решили разделить их поровну. Определите, сколько орешков достанется каждой белочке.
#
# Формат входных данных
# На вход дается два целых положительных числа N и K, каждое из которых не превышает 10000.
n, k = [int(x) for x in input().split()]
print(k // n)
# Задача 2.
# Белочки и орешки - 2
# N белочек нашли K орешков и решили разделить их поровну.
# Определите, сколько орешков останется после того, как все белочки возьмут себе равное количество орешков.
#
# Формат входных данных
# На вход дается два целых положительных числа N и K, каждое из которых не превышает 10000.
n , k = [int(x) for x in input().split()]
print(k % n)
# Задача 3
# Число десятков
# Дано целое неотрицательное число N, определите число десятков в нем (предпоследнюю цифру числа).
# Если предпоследней цифры нет, то можно считать, что число десятков равно нулю.
#
# Формат входных данных
# На вход дается целое положительное число N (0 ≤ N ≤ 1000000).
n = int(input())
print((n % 100) // 10)
# Задача 4
# Многочлен
# Дана функция y=x^6+2x^4-15x^3+3x^2-2.
# (Здесь знак ^ обозначает возведение в степень).
# Дано значение х, выведите значение данной функции в точке х
x = int(input())
print((x**6)+2*(x**4)-15*(x**3)+3*(x**2)-2)
# Задача 5
# Покупка пирожков
# Пирожок в столовой стоит А рублей и B копеек. Определите, сколько рублей и копеек нужно заплатить за N пирожков.
# Формат входных данных
# Программа получает на вход три числа: A, B, N - целые, положительные, не превышают 10000.
a, b, n = [int(x) for x in input().split()]
z1 = a * n
z2 = b * n
if z2 >= 100:
    z1 = z1 + (z2 // 100)
    z2 = z2 % 100
    print(z1 , z2)
else:
    print(z1 , z2)
